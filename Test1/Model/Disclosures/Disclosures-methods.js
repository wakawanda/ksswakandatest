model.Disclosures.collectionMethods.addToCollection = function(findStr) {	currentCol = this;	c2 = ds.Disclosures.query('Title == :1', '*' + findStr + '*');		c3 = currentCol.or(c2);	return c3;};model.Disclosures.collectionMethods.addToCollection.scope = "public";model.Disclosures.collectionMethods.removeFromColl = function(findStr) {	currentColl = this;	c2 = ds.Disclosures.query('Title == :1', '*' + findStr + '*');		c3 = currentColl.minus(c2);	return c3;};model.Disclosures.collectionMethods.removeFromColl.scope = "public";model.Disclosures.collectionMethods.omitSelected = function(inSelectedRows) {	var coll_Disclosures = this;	 	var toDelete = ds.Disclosures.createEntityCollection();           // Fill the collection to delete    inSelectedRows.forEach(function(rowNum) {        toDelete.add(coll_Disclosures[rowNum]);    });        //var test;    // Reduce current collection    //var newColl = coll_Disclosures.minus(toDelete);    // Delete what needs to be deleted    //toDelete.remove();    // Return the new collection    var newcoll = coll_Disclosures.minus(toDelete);    return newcoll;};model.Disclosures.collectionMethods.omitSelected.scope = "public";
model.Groups.collectionMethods.removeGroups = function(inSelectedRows) {	var coll_Groups = this;	 	var toDelete = ds.Groups.createEntityCollection();           // Fill the collection to delete    inSelectedRows.forEach(function(rowNum) {        toDelete.add(coll_Groups[rowNum]);    });        // Delete what needs to be deleted    toDelete.remove();    // Return the new collection    //var newcoll = coll_Groups.minus(toDelete);    //return newcoll;};model.Groups.collectionMethods.removeGroups.scope = "public";